name: E2E

on:
  workflow_dispatch:
    inputs:
      url:
        required: false
        description: The URL to run the test suite against. If omitted, it runs against production.
        type: string
  deployment_status:
  workflow_call:

permissions:
  contents: read

jobs:
  playwright:
    # 论坛真实浏览器冒烟（dev server + mock Gitee API），PR 与手动触发时运行
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'workflow_call' || (github.event.deployment_status.state == 'success' && github.event.deployment.environment == 'Production')
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node.js, pnpm and Python toolchain
        uses: ./.github/actions/setup

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run forum smoke tests
        run: npx playwright test

  lighthouse:
    environment:
      name: ${{ github.event.deployment.environment || 'Production' }}
      url: ${{ steps.get-url.outputs.test_url }}
    # 只对生产部署成功事件跑 lighthouse（手动触发不受限）
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event.deployment_status.state == 'success' &&
       github.event.deployment.environment == 'Production')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Determine test URL
        id: get-url
        run: |
          DEFAULT_URL="https://yuanshen.site/docs/"

          MANUAL_URL="${{ github.event.inputs.url }}"
          DEPLOYMENT_URL="${{ github.event.deployment_status.target_url }}"

          if [[ -n "$MANUAL_URL" ]]; then
            TEST_URL="$MANUAL_URL"
          elif [[ -n "$DEPLOYMENT_URL" && "$DEPLOYMENT_URL" =~ ^https://yuanshen\.site ]]; then
            # 仅使用 yuanshen.site 域名下的部署地址
            TEST_URL="$DEPLOYMENT_URL"
          else
            TEST_URL="$DEFAULT_URL"
          fi

          if [[ ! "$TEST_URL" =~ ^https?:// ]]; then
            echo "Invalid URL format: $TEST_URL"
            exit 1
          fi

          echo "test_url=$TEST_URL" >> $GITHUB_OUTPUT

      - name: Audit URLs using Lighthouse
        uses: treosh/lighthouse-ci-action@v12
        with:
          urls: ${{ steps.get-url.outputs.test_url }}
          uploadArtifacts: true
          temporaryPublicStorage: true
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
